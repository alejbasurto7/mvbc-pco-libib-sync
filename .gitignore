# Secrets & local config
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
build/
dist/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Phase-0 captured payloads (raw, with PII)
pco_sample.json
libib_sample.json

# Local migration artifacts
migration_log.jsonl
cards/
.test-card/

# Blast email pipeline — patron CSVs and per-blast state contain PII
Patrons-Status-*.csv
state/blast_*/

# Local sync logs (contain real PII; production state lives on the `state` branch)
state/sync_log/*.jsonl

# Commercial brand fonts — MVBC licenses these for mvbchurch.org. Do not redistribute.
# Open Sans (SIL OFL) is fine to commit.
fonts/Geometria-*.woff
fonts/Geometria-*.woff2
fonts/Geometria-*.ttf
fonts/Montserrat-*.ttf

# Local preview artifacts (regenerated by scratch scripts)
preview-card.png
preview-email.html
preview-email.txt
scratch_*.py
Joseph-Shanahan-library-card.png
Joseph-Shanahan-VIP-dark.png
Joseph-Shanahan-VIP.png
